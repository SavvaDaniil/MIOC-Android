<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ds.miocnative">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".View.CourseActivity"
            android:theme="@style/AppTheme.NoActionbar"></activity>
        <activity
            android:name=".ViewModel.LessonActivity"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity
            android:name=".ViewModel.DisciplineActivity"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity
            android:name=".ViewModel.AboutCourseActivity"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity
            android:name=".ViewModel.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity
            android:name=".ViewModel.AuthenticationActivity"
            android:theme="@style/AppTheme.NoActionbar" />
        <activity
            android:name=".ViewModel.MainActivity"
            android:theme="@style/AppTheme.NoActionbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>